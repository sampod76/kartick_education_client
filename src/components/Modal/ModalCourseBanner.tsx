"use clint";
import React from "react";

export default function ModalCourseBanner({
  categoryId,
  setIsModalOpen,
}: {
  categoryId: string;
  setIsModalOpen: any;
}) {
  console.log(categoryId);
  return <div>ModalCourseBanner</div>;
}
